# Force LF line endings for shell scripts and YAML files
# This prevents CRLF issues when running on Linux/Docker from Windows
*.sh    text eol=lf
*.yaml  text eol=lf
*.yml   text eol=lf
.gitattributes text eol=lf

# Dotfiles — always LF (Linux configs)
dotfiles/* text eol=lf
